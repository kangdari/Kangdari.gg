import React from 'react';
import styled from 'styled-components';

const Nav = () => {
    return (
        <NavContainer>
            {/* 가이드 추천덱 천상계 덱 아이템 .... */}
            
        </NavContainer>
    );
};

const NavContainer = styled.div`

`

export default Nav;